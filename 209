        #暴力法
        m = len(nums)
        ans = m+1

        for i in range(m):
            judge = nums[i]
            if judge>=s: ans = 1
            for j in range(i+1,m):
                judge +=nums[j]
                 
                if judge >=s:
                                   
                    ans = min(ans,j-i+1)
        return 0 if ans ==m+1 else  ans

n 方　超出时间限制
