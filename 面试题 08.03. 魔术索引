class Solution:
    def findMagicIndex(self, nums: List[int]) -> int:
        ans = -1
        for index,num in enumerate(nums):
            if index==num:
                return num
        return ans
