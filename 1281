class Solution:
    def subtractProductAndSum(self, n: int) -> int:
        n = str(n)
        mul = 1
        add = 0
        for i in n:
            mul = mul * int(i)
            add = add + int(i)
        return mul-add
        # print(n)
