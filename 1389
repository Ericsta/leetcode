class Solution:
    def createTargetArray(self, nums: List[int], index: List[int]) -> List[int]:
        ret = []
        for i,num in zip(index,nums):
            ret.insert(i, num)
        return ret
